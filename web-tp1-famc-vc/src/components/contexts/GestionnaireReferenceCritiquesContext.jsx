import {createContext} from "react";

/**
 * Contexte pour le gestionnaire de références des critiques.
 */
const GestionnaireReferenceCritiquesContext = createContext(null);

export default GestionnaireReferenceCritiquesContext;