import {createContext} from "react";

/**
 * Contexte pour le gestionnaire de références des statistiques.
 */
const GestionnaireReferenceStatistiquesContext = createContext(null);

export default GestionnaireReferenceStatistiquesContext;