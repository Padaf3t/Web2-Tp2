import {createContext} from "react";

/**
 * Contexte pour le gestionnaire de références des produits.
 */
const GestionnaireReferenceProduitsContext = createContext(null);

export default GestionnaireReferenceProduitsContext;